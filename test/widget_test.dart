// 품은기도 앱 기본 테스트
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App launches successfully', (WidgetTester tester) async {
    // 테스트는 Supabase 초기화가 필요하므로 기본 테스트만 작성
    expect(true, isTrue);
  });
}
